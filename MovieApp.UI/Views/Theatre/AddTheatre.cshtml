@model MovieApp.Entity.TheatreModel
@{
    ViewData["Title"] = "AddTheatre";
}

<h1>AddTheatre</h1>
<form method="post">
    <div class="form-group">
        <label for="Firstname">Thetre Name</label>
        <input type="text" class="form-control" asp-for="ThreatreName">
    </div>
    <div class="form-group">
        <label for="Firstname">Threatre Play Name</label>
        <input type="text" class="form-control" asp-for="ThreatrePlayName">
    </div>
    <div class="form-group">
        <label for="Firstname">Thetre Capacity</label>
        <input type="text" class="form-control" asp-for="ThreatreCapacity">
    </div>
    <input type="submit" class="btn btn-primary" value="Add Theatre">
    <button type="button" class="btn btn-secondary">Reset</button>
    <hr />
    @if (ViewBag.status == "Ok")
    {
        <div class="alert alert-success">
            @ViewBag.message
        </div>
    }
    else if (ViewBag.status == "Error")
    {
        <div class="alert alert-danger">
            @ViewBag.message
        </div>
    }
    <hr />
</form>
